using System;

namespace Programacion_BuclesWhile
{
    internal class Program
    {
        // Método principal de la aplicación (punto de entrada)
        static void Main(string[] args)
        {
            // Se crea una instancia de la clase Menu
            Menu menu = new Menu();

            // Se llama al método que muestra el menú y gestiona las opciones del usuario
            menu.MenuProgram();
        }
    }
}
